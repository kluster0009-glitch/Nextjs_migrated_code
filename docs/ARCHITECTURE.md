# 🏗️ Next.js 15 Architecture Diagram

## Application Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         BROWSER (Client)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                  React Client Components                  │  │
│  │  ('use client' directive)                                 │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │  • Interactive UI (useState, useEffect)                   │  │
│  │  • Event Handlers (onClick, onChange)                     │  │
│  │  • Browser APIs (localStorage, window)                    │  │
│  │  • Client-side routing (useRouter, usePathname)          │  │
│  │  • Auth Context Consumer (useAuth)                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              ↕                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              React Server Components (RSC)                │  │
│  │  (default, no directive needed)                           │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │  • Static layouts and pages                               │  │
│  │  • Server-side data fetching                              │  │
│  │  • Direct database queries                                │  │
│  │  • No client-side JavaScript needed                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                    NEXT.JS 15 MIDDLEWARE LAYER                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  middleware.js                                                    │
│  ├─ Auth Session Management                                      │
│  ├─ Cookie Refresh                                               │
│  ├─ Protected Route Checks                                       │
│  └─ Redirect Logic                                               │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                      NEXT.JS APP ROUTER                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  src/app/                                                         │
│  │                                                               │
│  ├─ layout.jsx (Root Layout)                                     │
│  │  ├─ Providers Wrapper                                         │
│  │  ├─ Theme Provider                                            │
│  │  ├─ Auth Provider                                             │
│  │  ├─ React Query Provider                                      │
│  │  └─ Toast/Sonner                                              │
│  │                                                               │
│  ├─ page.jsx (Landing Page) ────────────→ Public                │
│  │                                                               │
│  ├─ auth/                                                         │
│  │  ├─ page.jsx ───────────────────────→ Public                 │
│  │  └─ callback/page.jsx ──────────────→ OAuth Handler          │
│  │                                                               │
│  └─ (protected)/ ← Route Group with Shared Layout               │
│     │                                                            │
│     ├─ layout.jsx                                                │
│     │  └─ AppSidebar (Navigation)                               │
│     │                                                            │
│     ├─ feed/page.jsx ───────────────────→ Protected             │
│     ├─ chat/page.jsx ───────────────────→ Protected             │
│     ├─ profile/page.jsx ────────────────→ Protected             │
│     ├─ settings/page.jsx ───────────────→ Protected             │
│     ├─ qa/page.jsx ─────────────────────→ Protected             │
│     ├─ events/page.jsx ─────────────────→ Protected             │
│     ├─ library/page.jsx ────────────────→ Protected             │
│     ├─ professor/page.jsx ──────────────→ Protected             │
│     ├─ leaderboard/page.jsx ────────────→ Protected             │
│     └─ notifications/page.jsx ──────────→ Protected             │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                     SUPABASE INTEGRATION LAYER                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  Client-side: src/lib/supabase/client.js                         │
│  ├─ Browser Supabase Client                                      │
│  ├─ Used in Client Components                                    │
│  └─ Cookie-based session management                              │
│                                                                   │
│  Server-side: src/lib/supabase/server.js                         │
│  ├─ Server Supabase Client                                       │
│  ├─ Used in Server Components & API Routes                       │
│  └─ Direct cookie access via Next.js                             │
│                                                                   │
│  Middleware: src/lib/supabase/middleware.js                      │
│  ├─ Session refresh helper                                       │
│  └─ Used by middleware.js                                        │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                         SUPABASE BACKEND                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  PostgreSQL  │  │     Auth     │  │   Storage    │          │
│  │   Database   │  │   Service    │  │   Service    │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                   │
│  • Profiles Table                                                │
│  • Posts Table                                                   │
│  • Comments Table                                                │
│  • Likes Table                                                   │
│  • Real-time Subscriptions                                       │
│  • Row Level Security (RLS)                                      │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagrams

### Authentication Flow

```
┌─────────────┐
│   Browser   │
└──────┬──────┘
       │ 1. User visits /feed
       ↓
┌─────────────────────┐
│    middleware.js    │
│  Check session via  │
│  Supabase cookies   │
└──────┬──────────────┘
       │
       ├─ No session? ───────→ Redirect to /auth
       │
       ├─ Has session? ──────→ Refresh session
       │                       │
       ↓                       ↓
┌─────────────────────┐  ┌─────────────────────┐
│   Protected Page    │  │  Update cookies     │
│   Renders with      │←─│  Continue to page   │
│   User context      │  └─────────────────────┘
└─────────────────────┘
```

### Data Fetching Flow (Feed Page)

```
┌──────────────────┐
│  Feed Page (CSR) │
│  'use client'    │
└────────┬─────────┘
         │ 1. Component mounts
         ↓
┌─────────────────────────┐
│  useEffect() triggers   │
│  fetchPosts()           │
└────────┬────────────────┘
         │ 2. Create Supabase client
         ↓
┌─────────────────────────┐
│  createClient()         │
│  from client.js         │
└────────┬────────────────┘
         │ 3. Query database
         ↓
┌─────────────────────────┐
│  Supabase               │
│  .from('posts')         │
│  .select('*')           │
└────────┬────────────────┘
         │ 4. Returns data
         ↓
┌─────────────────────────┐
│  setPosts(data)         │
│  Render posts           │
└─────────────────────────┘
         │ 5. Setup real-time subscription
         ↓
┌─────────────────────────┐
│  supabase.channel()     │
│  Listen for changes     │
│  Auto-refresh on update │
└─────────────────────────┘
```

### Component Rendering Flow

```
┌───────────────────────────────────────────────────┐
│              Initial Page Request                  │
└────────────────────┬──────────────────────────────┘
                     │
                     ↓
┌───────────────────────────────────────────────────┐
│         Next.js Server (SSR/SSG)                  │
├───────────────────────────────────────────────────┤
│                                                    │
│  1. Load layout.jsx (Server Component)            │
│     ├─ Render HTML structure                      │
│     └─ Inject Providers (will hydrate)           │
│                                                    │
│  2. Load page.jsx                                 │
│     ├─ If Server Component: Fetch data, render   │
│     └─ If Client Component: Send to browser      │
│                                                    │
│  3. Send HTML + JavaScript bundle                 │
│                                                    │
└────────────────────┬──────────────────────────────┘
                     │
                     ↓
┌───────────────────────────────────────────────────┐
│              Browser (Hydration)                   │
├───────────────────────────────────────────────────┤
│                                                    │
│  1. Display HTML (instant)                        │
│                                                    │
│  2. Load JavaScript                               │
│                                                    │
│  3. Hydrate React components                      │
│     ├─ Attach event listeners                     │
│     ├─ Initialize state                           │
│     └─ Make page interactive                      │
│                                                    │
│  4. Client Components take over                   │
│     └─ useState, useEffect, etc.                  │
│                                                    │
└───────────────────────────────────────────────────┘
```

---

## Routing Structure

```
/                                 → Landing page (public)
                                    ├─ Shows hero, features
                                    └─ If logged in → redirect /feed

/auth                             → Authentication page (public)
                                    └─ Login/Signup forms

/auth/callback                    → OAuth callback (public)
                                    └─ Handles OAuth redirects

/feed                             → Feed page (protected)
                                    ├─ Requires auth
                                    ├─ Shows posts
                                    └─ Sidebar visible

/chat                             → Chat page (protected)
/profile                          → Profile page (protected)
/settings                         → Settings page (protected)
/qa                               → Q&A page (protected)
/events                           → Events page (protected)
/library                          → Library page (protected)
/professor                        → Professor page (protected)
/leaderboard                      → Leaderboard page (protected)
/notifications                    → Notifications page (protected)
```

---

## Component Hierarchy

```
RootLayout (app/layout.jsx)
│
├─ Providers (app/providers.jsx)
│  │
│  ├─ QueryClientProvider
│  │  └─ React Query setup
│  │
│  ├─ ThemeProvider
│  │  └─ next-themes (dark/light)
│  │
│  └─ AuthProvider
│     └─ User session management
│
├─ TooltipProvider
│  └─ shadcn/ui tooltips
│
├─ Toaster (Toast notifications)
│
└─ Children (page content)
   │
   ├─ Public Pages
   │  ├─ Landing (/)
   │  └─ Auth (/auth)
   │
   └─ Protected Layout (app/(protected)/layout.jsx)
      │
      ├─ SidebarProvider
      │  └─ Sidebar state management
      │
      ├─ AppSidebar
      │  ├─ Navigation menu
      │  ├─ User profile
      │  ├─ Theme toggle
      │  └─ Logout button
      │
      └─ Main Content Area
         ├─ Feed page
         ├─ Chat page
         ├─ Profile page
         └─ Other protected pages
```

---

## State Management Architecture

```
┌────────────────────────────────────────────────────┐
│              Global State (Context)                 │
├────────────────────────────────────────────────────┤
│                                                     │
│  AuthContext (src/lib/auth-context.jsx)            │
│  ├─ user                                            │
│  ├─ session                                         │
│  ├─ loading                                         │
│  └─ signOut()                                       │
│                                                     │
└────────────────────────────────────────────────────┘
                       ↕
┌────────────────────────────────────────────────────┐
│         Server State (React Query)                  │
├────────────────────────────────────────────────────┤
│                                                     │
│  QueryClientProvider                                │
│  ├─ Cache management                                │
│  ├─ Automatic refetching                            │
│  ├─ Background updates                              │
│  └─ Optimistic updates                              │
│                                                     │
└────────────────────────────────────────────────────┘
                       ↕
┌────────────────────────────────────────────────────┐
│           Local State (useState)                    │
├────────────────────────────────────────────────────┤
│                                                     │
│  Component-specific state                           │
│  ├─ Form inputs                                     │
│  ├─ UI toggles                                      │
│  ├─ Modal states                                    │
│  └─ Temporary data                                  │
│                                                     │
└────────────────────────────────────────────────────┘
```

---

## Build & Deployment Flow

```
┌─────────────────────┐
│   Source Code       │
│   (src/)            │
└──────┬──────────────┘
       │
       │ npm run build
       ↓
┌─────────────────────┐
│   Next.js Compiler  │
│   • React Compiler  │
│   • Minification    │
│   • Code Splitting  │
└──────┬──────────────┘
       │
       ↓
┌─────────────────────────────────────┐
│        Build Output (.next/)         │
├─────────────────────────────────────┤
│                                      │
│  ├─ Server Components                │
│  │  └─ Rendered on server            │
│  │                                    │
│  ├─ Client Components                │
│  │  └─ Hydrated in browser           │
│  │                                    │
│  ├─ Static Assets                    │
│  │  └─ CSS, images, fonts            │
│  │                                    │
│  ├─ API Routes                       │
│  │  └─ Serverless functions          │
│  │                                    │
│  └─ Middleware                       │
│     └─ Edge runtime                  │
│                                      │
└──────────────────┬──────────────────┘
                   │
                   │ Deploy
                   ↓
┌─────────────────────────────────────┐
│         Production Server            │
│         (Vercel/Other)               │
├─────────────────────────────────────┤
│                                      │
│  Edge Locations                      │
│  ├─ Middleware runs                  │
│  └─ CDN cached assets               │
│                                      │
│  Serverless Functions                │
│  ├─ API routes                       │
│  └─ Server Components                │
│                                      │
└─────────────────────────────────────┘
```

---

## Security Architecture

```
┌─────────────────────────────────────────────────┐
│              Security Layers                     │
├─────────────────────────────────────────────────┤
│                                                  │
│  Layer 1: Middleware (middleware.js)            │
│  ├─ Session validation                          │
│  ├─ Cookie refresh                              │
│  └─ Route protection                            │
│                                                  │
│  Layer 2: Supabase RLS                          │
│  ├─ Row Level Security policies                 │
│  ├─ Database-level protection                   │
│  └─ User-specific data access                   │
│                                                  │
│  Layer 3: Client-side Auth Checks               │
│  ├─ useAuth() hook validation                   │
│  ├─ UI-level protection                         │
│  └─ Redirect logic                              │
│                                                  │
│  Layer 4: Environment Variables                 │
│  ├─ Secrets in .env.local                       │
│  ├─ Server-only variables                       │
│  └─ No exposure to client                       │
│                                                  │
└─────────────────────────────────────────────────┘
```

---

## Performance Optimization Strategy

```
1. Code Splitting
   ├─ Automatic by Next.js
   ├─ Route-based chunks
   └─ Dynamic imports for heavy components

2. React Compiler
   ├─ Automatic memoization
   ├─ Optimized re-renders
   └─ No manual useMemo/useCallback needed

3. Image Optimization
   ├─ next/image component
   ├─ Automatic WebP conversion
   └─ Lazy loading

4. Caching Strategy
   ├─ Static Generation (SSG)
   ├─ Incremental Static Regeneration (ISR)
   └─ CDN caching

5. Database Optimization
   ├─ Supabase connection pooling
   ├─ Indexed queries
   └─ Real-time subscriptions

6. Bundle Size
   ├─ Tree shaking
   ├─ Code minification
   └─ Gzip compression
```

---

## Technology Stack Visualization

```
┌─────────────────────────────────────────────────┐
│                  Frontend                        │
├─────────────────────────────────────────────────┤
│  React 18 + Next.js 15                          │
│  ├─ React Compiler                              │
│  ├─ App Router                                  │
│  └─ Server Components                           │
├─────────────────────────────────────────────────┤
│  UI Libraries                                    │
│  ├─ Tailwind CSS                                │
│  ├─ shadcn/ui (Radix UI)                        │
│  ├─ NextUI                                      │
│  ├─ Framer Motion                               │
│  └─ Lucide Icons                                │
├─────────────────────────────────────────────────┤
│  State Management                                │
│  ├─ React Query                                 │
│  ├─ React Context                               │
│  └─ useState/useReducer                         │
└─────────────────────────────────────────────────┘
                      ↕
┌─────────────────────────────────────────────────┐
│                  Backend                         │
├─────────────────────────────────────────────────┤
│  Supabase                                        │
│  ├─ PostgreSQL Database                         │
│  ├─ Authentication Service                      │
│  ├─ Storage Service                             │
│  ├─ Real-time Subscriptions                     │
│  └─ Row Level Security                          │
└─────────────────────────────────────────────────┘
                      ↕
┌─────────────────────────────────────────────────┐
│              Development Tools                   │
├─────────────────────────────────────────────────┤
│  ├─ ESLint (Code quality)                       │
│  ├─ PostCSS (CSS processing)                    │
│  ├─ React Hook Form (Forms)                     │
│  ├─ Zod (Validation)                            │
│  └─ date-fns (Date utilities)                   │
└─────────────────────────────────────────────────┘
```

---

This architecture provides:
- **Scalability**: Easy to add new routes and features
- **Performance**: Optimized builds and automatic code splitting
- **Security**: Multiple layers of protection
- **Maintainability**: Clear separation of concerns
- **Developer Experience**: Modern tooling and hot reload
